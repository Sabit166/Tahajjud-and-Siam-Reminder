services:
  dhikr-bot:
    build: .
    container_name: dhikr-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/dhikr_records.db
    volumes:
      - ./data:/data
